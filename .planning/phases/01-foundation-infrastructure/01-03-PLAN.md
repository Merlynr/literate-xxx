---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - wx-fe/package.json
  - wx-fe/tsconfig.json
  - wx-fe/vite.config.ts
  - wx-fe/src/pages.json
  - wx-fe/src/App.vue
  - wx-fe/src/main.ts
  - wx-fe/src/manifest.json
  - wx-fe/src/pages/index/index.vue
  - wx-fe/src/pages/generate/index.vue
  - wx-fe/src/pages/my/index.vue
  - wx-fe/src/stores/index.ts
  - wx-fe/src/stores/user.ts
  - wx-fe/src/api/request.ts
  - wx-fe/src/env.d.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Uni-app project created with Vue3 + Vite + TypeScript"
    - "Three TabBar pages render correctly (首页 / 生成 / 我的)"
    - "API request wrapper configured with BFF base URL"
    - "Pinia store initializes without errors"
    - "mp-weixin compilation produces valid output directory"
  artifacts:
    - path: "wx-fe/package.json"
      provides: "Frontend project dependencies"
      contains: "pinia"
    - path: "wx-fe/src/pages.json"
      provides: "Page routes and TabBar configuration"
      contains: "tabBar"
    - path: "wx-fe/src/pages/index/index.vue"
      provides: "Home page component"
    - path: "wx-fe/src/pages/generate/index.vue"
      provides: "Generation wizard page component"
    - path: "wx-fe/src/pages/my/index.vue"
      provides: "Profile/My page component"
    - path: "wx-fe/src/api/request.ts"
      provides: "HTTP request wrapper with BFF base URL"
      exports: ["request"]
    - path: "wx-fe/src/stores/index.ts"
      provides: "Pinia store initialization"
  key_links:
    - from: "wx-fe/src/pages.json"
      to: "wx-fe/src/pages/"
      via: "page path registration"
      pattern: "pages/"
    - from: "wx-fe/src/api/request.ts"
      to: "wx-fe/src/manifest.json"
      via: "BFF base URL"
      pattern: "baseUrl|baseURL"
    - from: "wx-fe/src/App.vue"
      to: "wx-fe/src/stores/index.ts"
      via: "Pinia initialization"
      pattern: "createPinia|useStore"
---

<objective>
Scaffold the Uni-app WeChat Mini Program frontend for XX甄选.

Purpose: Establish the frontend shell that Phase 2+ will build upon. Delivers a compilable mp-weixin project with three TabBar pages, Pinia state management, and an HTTP request wrapper pointing at the BFF API. This validates the entire frontend toolchain (Uni-app + Vue3 + Vite + TypeScript) compiles to WeChat Mini Program format.

Output: A `wx-fe/` project that compiles to `dist/build/mp-weixin/` and displays a three-tab layout.
</objective>

<execution_context>
@C:/Users/101202106068/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/101202106068/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md

<!-- Plan 01-01 created the backend foundation. This plan focuses on frontend only. -->
<!-- The BFF health check endpoint (Plan 01-01) is what this frontend will eventually call. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Uni-app Project + Configure TabBar + Pinia</name>
  <files>
    wx-fe/package.json,
    wx-fe/tsconfig.json,
    wx-fe/vite.config.ts,
    wx-fe/src/pages.json,
    wx-fe/src/App.vue,
    wx-fe/src/main.ts,
    wx-fe/src/manifest.json,
    wx-fe/src/env.d.ts,
    wx-fe/src/pages/index/index.vue,
    wx-fe/src/pages/generate/index.vue,
    wx-fe/src/pages/my/index.vue,
    wx-fe/src/stores/index.ts,
    wx-fe/src/stores/user.ts
  </files>
  <action>
**Prerequisites:** Node.js 18+ must be installed. If current Node.js is 11.x, upgrade first:
```powershell
# Check version
node -v
# If < 18, install Node.js 20 LTS from https://nodejs.org/
```

**Step 1: Create Uni-app project (per D-08)**

```powershell
cd F:\project\xxx
npx degit dcloudio/uni-preset-vue#vite-ts wx-fe
```

If `degit` fails, fallback:
```powershell
git clone --branch vite-ts --single-branch https://github.com/nicepkg/uni-preset-vue wx-fe-temp
Move-Item wx-fe-temp/* wx-fe/
Move-Item wx-fe-temp/.* wx-fe/
Remove-Item wx-fe-temp -Recurse -Force
```

**Step 2: Install dependencies**

```powershell
cd wx-fe
npm install
npm install pinia
npm install -D @types/node
```

**Step 3: Configure TabBar — `wx-fe/src/pages.json`**

Per D-10: Three TabBar pages (首页 / 生成 / 我的):

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {
        "navigationBarTitleText": "首页"
      }
    },
    {
      "path": "pages/generate/index",
      "style": {
        "navigationBarTitleText": "AI生成"
      }
    },
    {
      "path": "pages/my/index",
      "style": {
        "navigationBarTitleText": "我的"
      }
    }
  ],
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "XX甄选",
    "navigationBarBackgroundColor": "#FFFFFF",
    "backgroundColor": "#F5F5F5"
  },
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#1AAD19",
    "borderStyle": "black",
    "backgroundColor": "#FFFFFF",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "static/tab/home.png",
        "selectedIconPath": "static/tab/home-active.png"
      },
      {
        "pagePath": "pages/generate/index",
        "text": "生成",
        "iconPath": "static/tab/generate.png",
        "selectedIconPath": "static/tab/generate-active.png"
      },
      {
        "pagePath": "pages/my/index",
        "text": "我的",
        "iconPath": "static/tab/my.png",
        "selectedIconPath": "static/tab/my-active.png"
      }
    ]
  }
}
```

**NOTE:** TabBar icons. Create placeholder 81x81 PNG files at `wx-fe/src/static/tab/`. Use simple colored squares as placeholders:
- home.png / home-active.png
- generate.png / generate-active.png
- my.png / my-active.png

Generate with a script or copy from the Uni-app template defaults. The real icons will be designed in a later phase. For now, any valid PNG works.

**Step 4: Create page components**

`wx-fe/src/pages/index/index.vue` (首页 — Home):

```vue
<template>
  <view class="page-home">
    <view class="hero">
      <text class="title">XX甄选</text>
      <text class="subtitle">AI商品宣传图生成</text>
    </view>
    <view class="features">
      <view class="feature-card">
        <text class="feature-icon">📸</text>
        <text class="feature-title">拍照上传</text>
        <text class="feature-desc">拍摄商品实物照片</text>
      </view>
      <view class="feature-card">
        <text class="feature-icon">✨</text>
        <text class="feature-title">AI生成</text>
        <text class="feature-desc">一键生成宣传图</text>
      </view>
      <view class="feature-card">
        <text class="feature-icon">💾</text>
        <text class="feature-title">下载使用</text>
        <text class="feature-desc">保存到相册直接使用</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
// Home page - Phase 2+ will add auth check and quota display
</script>

<style scoped>
.page-home {
  padding: 32rpx;
}
.hero {
  text-align: center;
  padding: 60rpx 0;
}
.title {
  font-size: 48rpx;
  font-weight: bold;
  display: block;
}
.subtitle {
  font-size: 28rpx;
  color: #666;
  display: block;
  margin-top: 16rpx;
}
.features {
  display: flex;
  gap: 24rpx;
  margin-top: 40rpx;
}
.feature-card {
  flex: 1;
  background: #fff;
  border-radius: 16rpx;
  padding: 32rpx 16rpx;
  text-align: center;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.05);
}
.feature-icon {
  font-size: 48rpx;
  display: block;
}
.feature-title {
  font-size: 28rpx;
  font-weight: 500;
  display: block;
  margin-top: 16rpx;
}
.feature-desc {
  font-size: 22rpx;
  color: #999;
  display: block;
  margin-top: 8rpx;
}
</style>
```

`wx-fe/src/pages/generate/index.vue` (生成 — Generation wizard placeholder):

```vue
<template>
  <view class="page-generate">
    <view class="coming-soon">
      <text class="icon">🎨</text>
      <text class="title">AI生成向导</text>
      <text class="desc">选择类目 → 选择风格 → 上传照片 → 生成宣传图</text>
      <text class="hint">Phase 3 将实现完整向导流程</text>
    </view>
  </view>
</template>

<script setup lang="ts">
// Generation wizard - Phase 3 will implement full flow
</script>

<style scoped>
.page-generate {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 80vh;
}
.coming-soon {
  text-align: center;
}
.icon {
  font-size: 80rpx;
  display: block;
}
.title {
  font-size: 36rpx;
  font-weight: bold;
  display: block;
  margin-top: 24rpx;
}
.desc {
  font-size: 26rpx;
  color: #666;
  display: block;
  margin-top: 16rpx;
}
.hint {
  font-size: 22rpx;
  color: #999;
  display: block;
  margin-top: 32rpx;
}
</style>
```

`wx-fe/src/pages/my/index.vue` (我的 — Profile):

```vue
<template>
  <view class="page-my">
    <view class="header">
      <view class="avatar-placeholder">
        <text>👤</text>
      </view>
      <text class="username">未登录</text>
    </view>
    <view class="menu">
      <view class="menu-item">
        <text>我的作品</text>
        <text class="arrow">›</text>
      </view>
      <view class="menu-item">
        <text>额度明细</text>
        <text class="arrow">›</text>
      </view>
      <view class="menu-item">
        <text>关于我们</text>
        <text class="arrow">›</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
// Profile page - Phase 2 will add WeChat login, Phase 4 will add works/credits
</script>

<style scoped>
.page-my {
  padding: 0;
}
.header {
  background: linear-gradient(135deg, #1AAD19, #12B7F5);
  padding: 60rpx 32rpx 40rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
}
.avatar-placeholder {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
}
.username {
  font-size: 32rpx;
  color: #fff;
  font-weight: 500;
}
.menu {
  margin-top: 24rpx;
  background: #fff;
}
.menu-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx;
  border-bottom: 1rpx solid #f0f0f0;
  font-size: 28rpx;
}
.arrow {
  color: #ccc;
  font-size: 32rpx;
}
</style>
```

**Step 5: Initialize Pinia (per D-09)**

`wx-fe/src/stores/index.ts`:

```typescript
import { createPinia } from 'pinia'

const pinia = createPinia()

export default pinia
```

`wx-fe/src/stores/user.ts`:

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useUserStore = defineStore('user', () => {
  const isLoggedIn = ref(false)
  const nickname = ref('')
  const tenantId = ref('')

  function setUser(data: { nickname: string; tenantId: string }) {
    isLoggedIn.value = true
    nickname.value = data.nickname
    tenantId.value = data.tenantId
  }

  function logout() {
    isLoggedIn.value = false
    nickname.value = ''
    tenantId.value = ''
  }

  return { isLoggedIn, nickname, tenantId, setUser, logout }
})
```

**Step 6: Update `wx-fe/src/main.ts` to use Pinia**

```typescript
import { createSSRApp } from 'vue'
import App from './App.vue'
import pinia from './stores'

export function createApp() {
  const app = createSSRApp(App)
  app.use(pinia)
  return { app }
}
```

**Step 7: Update `wx-fe/src/App.vue`**

```vue
<script setup lang="ts">
import { onLaunch, onShow, onHide } from '@dcloudio/uni-app'

onLaunch(() => {
  console.log('XX甄选 App Launch')
})

onShow(() => {
  console.log('App Show')
})

onHide(() => {
  console.log('App Hide')
})
</script>

<style>
page {
  background-color: #f5f5f5;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
</style>
```

**Step 8: Create `wx-fe/src/env.d.ts`** for TypeScript declarations:

```typescript
/// <reference types="@dcloudio/types" />

declare namespace UniApp {
  interface Uni {
    // Extend as needed
  }
}
```
  </action>
  <verify>
    <automated>cd wx-fe && npm run build:mp-weixin 2>&1 | Select-String -Pattern "Build|error|ERROR|compiled" | Select-Object -First 10</automated>
  </verify>
  <done>
    - Uni-app project created at wx-fe/ with Vue3 + Vite + TypeScript (D-08)
    - pages.json defines three TabBar pages: 首页 / 生成 / 我的 (D-10)
    - Each page has a Vue component with basic layout
    - Pinia installed and initialized in main.ts (D-09)
    - user store defined with login/logout state management
    - npm run build:mp-weixin compiles without errors
    - dist/build/mp-weixin/ directory exists with app.json, pages/, etc.
  </done>
</task>

<task type="auto">
  <name>Task 2: API Request Wrapper + Build Verification</name>
  <files>
    wx-fe/src/api/request.ts
  </files>
  <action>
**Step 1: Create `wx-fe/src/api/request.ts`**

HTTP request wrapper using uni.request with BFF base URL. Designed for Phase 2+ API calls:

```typescript
/**
 * HTTP request wrapper for XX甄选 BFF API.
 *
 * Uses uni.request for WeChat Mini Program compatibility.
 * Base URL configured per environment — no hardcoded URLs.
 */

interface RequestOptions {
  url: string
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE'
  data?: Record<string, unknown> | unknown
  header?: Record<string, string>
}

interface ApiResponse<T = unknown> {
  code: number
  data: T
  message: string
}

// BFF base URL — update per environment
// WeChat Mini Program requires HTTPS in production; HTTP OK for dev
const BASE_URL = 'http://localhost:8000/api/v1'

// Dev mode flag — in production, use uni.getStorageSync for token
let authToken = ''

export function setToken(token: string) {
  authToken = token
}

export function request<T = unknown>(options: RequestOptions): Promise<ApiResponse<T>> {
  return new Promise((resolve, reject) => {
    const header: Record<string, string> = {
      'Content-Type': 'application/json',
      ...options.header,
    }

    if (authToken) {
      header['Authorization'] = `Bearer ${authToken}`
    }

    uni.request({
      url: `${BASE_URL}${options.url}`,
      method: options.method || 'GET',
      data: options.data,
      header,
      success: (res) => {
        if (res.statusCode >= 200 && res.statusCode < 300) {
          resolve(res.data as ApiResponse<T>)
        } else if (res.statusCode === 401) {
          // Token expired — Phase 2 will implement refresh logic
          uni.showToast({ title: '请重新登录', icon: 'none' })
          reject(new Error('Unauthorized'))
        } else {
          const errMsg = (res.data as { message?: string })?.message || '请求失败'
          uni.showToast({ title: errMsg, icon: 'none' })
          reject(new Error(errMsg))
        }
      },
      fail: (err) => {
        uni.showToast({ title: '网络异常', icon: 'none' })
        reject(err)
      },
    })
  })
}

export default request
```

**Step 2: Verify full build**

```powershell
cd wx-fe
npm run build:mp-weixin
```

Expected: Build succeeds, `dist/build/mp-weixin/` directory created with:
- `app.json` (contains tabBar config)
- `app.js`
- `pages/index/`, `pages/generate/`, `pages/my/` (each with .wxml, .wxss, .js, .json)
- `static/tab/` (icon images)

**Step 3: Verify output structure**

```powershell
cd wx-fe
Get-ChildItem -Path dist/build/mp-weixin/ -Recurse -Name | Select-Object -First 30
```

Verify:
- `app.json` contains `"tabBar"` with 3 items
- Each page directory exists under `pages/`

**Step 4: Verify app.json content**

```powershell
Get-Content wx-fe/dist/build/mp-weixin/app.json | ConvertFrom-Json | Select-Object -ExpandProperty tabBar
```

Should show 3 tabBar items with correct pagePath values.
  </action>
  <verify>
    <automated>if (Test-Path "wx-fe/dist/build/mp-weixin/app.json") { Get-Content wx-fe/dist/build/mp-weixin/app.json | ConvertFrom-Json | ForEach-Object { if ($_.tabBar.list.Count -eq 3) { "OK: 3 TabBar pages" } else { "FAIL: Expected 3 TabBar pages" } } } else { "FAIL: app.json not found" }</automated>
  </verify>
  <done>
    - request.ts exports a request() function with BFF base URL
    - Authorization header support (Bearer token)
    - 401 handling with toast message
    - Network error handling with toast
    - mp-weixin build succeeds
    - dist/build/mp-weixin/app.json contains tabBar with 3 items
    - All three page directories exist in build output
  </done>
</task>

</tasks>

<verification>
1. `cd wx-fe && npm run build:mp-weixin` compiles without errors
2. `dist/build/mp-weixin/app.json` exists and contains tabBar with 3 items (首页/生成/我的)
3. `dist/build/mp-weixin/pages/index/` directory exists with .wxml files
4. `dist/build/mp-weixin/pages/generate/` directory exists
5. `dist/build/mp-weixin/pages/my/` directory exists
6. Pinia store imports work without TypeScript errors
7. request.ts has no TypeScript compilation errors
</verification>

<success_criteria>
- Uni-app project created with Vue3 + Vite + TypeScript (D-08)
- Three TabBar pages defined in pages.json (D-10)
- Pinia state management initialized (D-09)
- HTTP request wrapper configured with BFF base URL
- mp-weixin compilation produces valid output
- TabBar configuration present in compiled app.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md`
</output>
