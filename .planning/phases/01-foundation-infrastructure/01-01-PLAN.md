---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python-bff/pyproject.toml
  - python-bff/.env.example
  - python-bff/alembic.ini
  - python-bff/alembic/env.py
  - python-bff/app/__init__.py
  - python-bff/app/main.py
  - python-bff/app/core/__init__.py
  - python-bff/app/core/config.py
  - python-bff/app/core/database.py
  - python-bff/app/core/redis_client.py
  - python-bff/app/models/__init__.py
  - python-bff/app/models/base.py
  - python-bff/app/schemas/__init__.py
  - python-bff/app/schemas/health.py
  - python-bff/app/api/__init__.py
  - python-bff/app/api/deps.py
  - python-bff/app/api/router.py
  - python-bff/app/api/v1/__init__.py
  - python-bff/app/api/v1/router.py
  - python-bff/app/api/v1/health.py
  - python-bff/app/api/v1/uploads.py
  - python-bff/app/api/v1/tasks.py
  - python-bff/app/services/__init__.py
  - python-bff/app/workers/__init__.py
  - python-bff/app/providers/__init__.py
  - python-bff/tests/__init__.py
  - python-bff/tests/conftest.py
  - python-bff/tests/test_health.py
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "FastAPI app starts via uvicorn without errors"
    - "GET /health returns 200 with DB connectivity check"
    - "Alembic can generate and run migrations against MySQL"
    - "All config values load from .env via pydantic-settings"
    - "Base model provides id/tenant_id/created_at/updated_at columns"
  artifacts:
    - path: "python-bff/pyproject.toml"
      provides: "Project metadata and all dependencies"
      contains: "fastapi"
    - path: "python-bff/app/core/config.py"
      provides: "Centralized Settings class"
      exports: ["settings"]
    - path: "python-bff/app/core/database.py"
      provides: "Async SQLAlchemy engine and session factory"
      exports: ["engine", "async_session_factory"]
    - path: "python-bff/app/main.py"
      provides: "FastAPI app factory with lifespan"
      exports: ["create_app", "app"]
    - path: "python-bff/app/api/v1/health.py"
      provides: "Health check endpoints"
      exports: ["router"]
    - path: "python-bff/app/models/base.py"
      provides: "Base ORM model with common columns"
      exports: ["Base"]
    - path: "python-bff/alembic/env.py"
      provides: "Async Alembic migration environment"
  key_links:
    - from: "python-bff/app/main.py"
      to: "python-bff/app/api/v1/router.py"
      via: "app.include_router"
      pattern: "include_router"
    - from: "python-bff/app/api/v1/health.py"
      to: "python-bff/app/core/database.py"
      via: "AsyncSession dependency"
      pattern: "get_db|Depends"
    - from: "python-bff/alembic/env.py"
      to: "python-bff/app/core/database.py"
      via: "import engine"
      pattern: "from app.core.database import"
    - from: "python-bff/app/core/config.py"
      to: "python-bff/.env.example"
      via: "pydantic-settings env loading"
      pattern: "DATABASE_URL|REDIS_URL"
---

<objective>
Build the complete FastAPI backend skeleton for XX甄选 BFF.

Purpose: Establish the backend foundation that all subsequent plans (Celery/OSS, frontend integration) build upon. Delivers a running FastAPI app with MySQL connectivity, Alembic migrations, health check endpoint, and the full directory structure per D-01.

Output: A working `python-bff/` project that starts with `uvicorn app.main:app`, responds to `GET /health`, and can run Alembic migrations against MySQL.
</objective>

<execution_context>
@C:/Users/101202106068/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/101202106068/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding + Config + Database + Alembic</name>
  <files>
    python-bff/pyproject.toml,
    python-bff/.env.example,
    python-bff/app/__init__.py,
    python-bff/app/core/__init__.py,
    python-bff/app/core/config.py,
    python-bff/app/core/database.py,
    python-bff/app/core/redis_client.py,
    python-bff/app/models/__init__.py,
    python-bff/app/models/base.py,
    python-bff/app/schemas/__init__.py,
    python-bff/app/services/__init__.py,
    python-bff/app/workers/__init__.py,
    python-bff/app/providers/__init__.py,
    python-bff/alembic.ini,
    python-bff/alembic/env.py
  </files>
  <action>
**Step 1: Create directory structure (per D-01 — 按层分目录)**

Create the following empty `__init__.py` files:
- `python-bff/app/__init__.py`
- `python-bff/app/core/__init__.py`
- `python-bff/app/models/__init__.py`
- `python-bff/app/schemas/__init__.py`
- `python-bff/app/services/__init__.py`
- `python-bff/app/workers/__init__.py`
- `python-bff/app/providers/__init__.py`

**Step 2: Create `python-bff/pyproject.toml`**

Use PEP 621 format. Pin versions per RESEARCH.md verified versions:

```toml
[project]
name = "xxzx-bff"
version = "0.1.0"
description = "XX甄选 BFF — FastAPI backend for AIGC product image generation"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136,<0.137",
    "uvicorn[standard]>=0.46,<0.47",
    "python-multipart>=0.0.28",
    "sqlalchemy[asyncio]>=2.0.49,<2.1",
    "aiomysql>=0.2,<0.3",
    "alembic>=1.18,<1.19",
    "pydantic-settings>=2.14,<2.15",
    "celery[redis]>=5.6,<5.7",
    "redis>=7.4,<7.5",
    "boto3>=1.43,<1.44",
    "pillow>=12.2,<13",
    "loguru>=0.7,<0.8",
    "tenacity>=9.1,<10",
    "httpx>=0.28,<0.29",
    "python-jose[cryptography]>=3.3,<4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

**Step 3: Create `python-bff/.env.example`**

All config values driven by env vars (per D-07):

```env
# Application
PROJECT_NAME=XX甄选 BFF
VERSION=0.1.0
DEBUG=true
API_V1_PREFIX=/api/v1

# MySQL (per D-02: local install, no Docker)
DATABASE_URL=mysql+aiomysql://root:root@localhost:3306/xxzx

# Redis (local install)
REDIS_URL=redis://localhost:6379/0

# Celery (per D-03: --pool=threads on Windows)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# S3/MinIO (per D-05: local MinIO, D-06: boto3 S3 protocol, D-07: env-driven)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=xxzx-assets
S3_REGION=
S3_USE_SSL=false
```

**Step 4: Create `python-bff/app/core/config.py`**

pydantic-settings BaseSettings (per RESEARCH.md Pattern 3):

```python
from __future__ import annotations
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=False,
    )

    # Application
    PROJECT_NAME: str = "XX甄选 BFF"
    VERSION: str = "0.1.0"
    DEBUG: bool = False
    API_V1_PREFIX: str = "/api/v1"

    # MySQL
    DATABASE_URL: str = "mysql+aiomysql://root:root@localhost:3306/xxzx"

    # Redis
    REDIS_URL: str = "redis://localhost:6379/0"

    # Celery
    CELERY_BROKER_URL: str = "redis://localhost:6379/1"
    CELERY_RESULT_BACKEND: str = "redis://localhost:6379/2"

    # S3 / MinIO (per D-05, D-06, D-07)
    S3_ENDPOINT: str = "http://localhost:9000"
    S3_ACCESS_KEY: str = "minioadmin"
    S3_SECRET_KEY: str = "minioadmin"
    S3_BUCKET: str = "xxzx-assets"
    S3_REGION: str = ""
    S3_USE_SSL: bool = False


settings = Settings()
```

**Step 5: Create `python-bff/app/core/database.py`**

Per RESEARCH.md Pattern 2 — async engine + session factory:

```python
from __future__ import annotations
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from app.core.config import settings

engine = create_async_engine(
    settings.DATABASE_URL,
    echo=settings.DEBUG,
    pool_pre_ping=True,
)

async_session_factory = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False,
)
```

**Step 6: Create `python-bff/app/core/redis_client.py`**

```python
from __future__ import annotations
import redis.asyncio as aioredis
from app.core.config import settings

redis_client = aioredis.from_url(settings.REDIS_URL, decode_responses=True)
```

**Step 7: Create `python-bff/app/models/base.py`**

Per phase goal: Base model with id/tenant_id/created_at/updated_at:

```python
from __future__ import annotations
import uuid
from datetime import datetime
from sqlalchemy import DateTime, func
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
    pass


class TenantModel(Base):
    """Base model for all tenant-scoped entities."""
    __abstract__ = True

    id: Mapped[uuid.UUID] = mapped_column(
        primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(index=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )
```

**Step 8: Set up Alembic (async mode)**

Create `python-bff/alembic.ini` — set `sqlalchemy.url` to empty (overridden in env.py):

```ini
[alembic]
script_location = alembic
prepend_sys_path = .

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

Create `python-bff/alembic/env.py` in async mode (per RESEARCH.md Pitfall 3):

```python
import asyncio
from logging.config import fileConfig
from sqlalchemy import pool
from sqlalchemy.ext.asyncio import async_engine_from_config
from alembic import context
from app.core.config import settings
from app.models.base import Base

# Import all models here so Alembic sees them
# from app.models import user, ...  # Add as models are created

config = context.config
config.set_main_option("sqlalchemy.url", settings.DATABASE_URL.replace("%", "%%"))

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection):
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


def run_migrations_online() -> None:
    asyncio.run(run_async_migrations())


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

**Step 9: Create venv + install deps + verify**

```powershell
cd python-bff
python -m venv .venv
.venv\Scripts\activate
pip install -e ".[dev]"
```

After install, verify imports:

```powershell
python -c "from app.core.config import settings; print(settings.PROJECT_NAME)"
python -c "from app.models.base import Base, TenantModel; print(Base.metadata.tables)"
```

**Step 10: Run initial Alembic migration**

```powershell
cd python-bff
.venv\Scripts\activate
alembic revision --autogenerate -m "initial_base_model"
alembic upgrade head
```

NOTE: MySQL must be running with database `xxzx` created. If not yet installed, create database first:
```powershell
# After MySQL installed:
mysql -u root -p -e "CREATE DATABASE xxzx CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;""
```
  </action>
  <verify>
    <automated>cd python-bff && .venv\Scripts\activate && python -c "from app.core.config import settings; assert settings.PROJECT_NAME == 'XX甄选 BFF'; from app.models.base import Base, TenantModel; assert 'id' in TenantModel.__annotations__; print('OK')"</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies pinned per RESEARCH.md
    - .env.example contains all environment variables (DB, Redis, Celery, S3)
    - Settings class loads from .env via pydantic-settings (D-07: env-driven)
    - Async SQLAlchemy engine configured with mysql+aiomysql://
    - Base model has id (UUID), tenant_id, created_at, updated_at columns
    - Alembic env.py uses async mode
    - `alembic revision --autogenerate` produces a migration file
    - `alembic upgrade head` runs successfully against MySQL
  </done>
</task>

<task type="auto">
  <name>Task 2: App Factory + Health API + Router + Tests</name>
  <files>
    python-bff/app/main.py,
    python-bff/app/api/__init__.py,
    python-bff/app/api/deps.py,
    python-bff/app/api/router.py,
    python-bff/app/api/v1/__init__.py,
    python-bff/app/api/v1/router.py,
    python-bff/app/api/v1/health.py,
    python-bff/app/api/v1/uploads.py,
    python-bff/app/api/v1/tasks.py,
    python-bff/app/schemas/health.py,
    python-bff/tests/__init__.py,
    python-bff/tests/conftest.py,
    python-bff/tests/test_health.py
  </files>
  <action>
**Step 1: Create `python-bff/app/api/deps.py`**

Shared dependencies:

```python
from __future__ import annotations
from typing import AsyncGenerator
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import async_session_factory


async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

**Step 2: Create `python-bff/app/schemas/health.py`**

```python
from __future__ import annotations
from pydantic import BaseModel


class HealthCheck(BaseModel):
    status: str
    checks: dict[str, str]
```

**Step 3: Create `python-bff/app/api/v1/health.py`**

Per RESEARCH.md Pattern 5 — health checks for DB, Redis, MinIO:

```python
from __future__ import annotations
from fastapi import APIRouter, Depends
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession
import redis.asyncio as aioredis
from app.api.deps import get_db
from app.core.config import settings
from app.schemas.health import HealthCheck

router = APIRouter()


@router.get("/health/liveness")
async def liveness():
    return {"status": "ok"}


@router.get("/health/readiness", response_model=HealthCheck)
async def readiness(db: AsyncSession = Depends(get_db)):
    checks: dict[str, str] = {}

    # Database check
    try:
        await db.execute(text("SELECT 1"))
        checks["database"] = "ok"
    except Exception as e:
        checks["database"] = f"error: {e}"

    # Redis check
    try:
        r = aioredis.from_url(settings.REDIS_URL)
        await r.ping()
        await r.close()
        checks["redis"] = "ok"
    except Exception as e:
        checks["redis"] = f"error: {e}"

    # MinIO/S3 check (boto3 sync call — acceptable for health check)
    try:
        import boto3
        from botocore.config import Config as BotoConfig
        s3 = boto3.client(
            "s3",
            endpoint_url=settings.S3_ENDPOINT,
            aws_access_key_id=settings.S3_ACCESS_KEY,
            aws_secret_access_key=settings.S3_SECRET_KEY,
            config=BotoConfig(signature_version="s3v4"),
        )
        s3.list_buckets()
        checks["minio"] = "ok"
    except Exception as e:
        checks["minio"] = f"error: {e}"

    all_ok = all(v == "ok" for v in checks.values())
    return HealthCheck(status="ok" if all_ok else "degraded", checks=checks)
```

**Step 4: Create stub endpoint files for Plan 01-02 to implement**

Create `python-bff/app/api/v1/uploads.py` (empty router placeholder):

```python
from __future__ import annotations
from fastapi import APIRouter

router = APIRouter()

# Plan 01-02 will implement: presigned upload/download endpoints
```

Create `python-bff/app/api/v1/tasks.py` (empty router placeholder):

```python
from __future__ import annotations
from fastapi import APIRouter

router = APIRouter()

# Plan 01-02 will implement: Celery task dispatch endpoints
```

**Step 5: Create router aggregation**

`python-bff/app/api/v1/router.py`:

```python
from fastapi import APIRouter
from app.api.v1.health import router as health_router
from app.api.v1.uploads import router as uploads_router
from app.api.v1.tasks import router as tasks_router

router = APIRouter()
router.include_router(health_router, tags=["health"])
router.include_router(uploads_router, tags=["uploads"])
router.include_router(tasks_router, tags=["tasks"])
```

`python-bff/app/api/router.py`:

```python
from fastapi import APIRouter
from app.api.v1.router import router as v1_router

api_router = APIRouter()
api_router.include_router(v1_router, prefix="/v1")
```

**Step 6: Create `python-bff/app/main.py`**

Per RESEARCH.md Pattern 1 — app factory with lifespan:

```python
from __future__ import annotations
from contextlib import asynccontextmanager
from fastapi import FastAPI
from app.core.config import settings
from app.core.database import engine
from app.api.router import api_router
from loguru import logger


@asynccontextmanager
async def lifespan(app: FastAPI):
    logger.info("Starting {} v{}", settings.PROJECT_NAME, settings.VERSION)
    yield
    logger.info("Shutting down...")
    await engine.dispose()


def create_app() -> FastAPI:
    app = FastAPI(
        title=settings.PROJECT_NAME,
        version=settings.VERSION,
        lifespan=lifespan,
    )
    app.include_router(api_router, prefix=settings.API_V1_PREFIX)
    return app


app = create_app()
```

**Step 7: Create tests**

`python-bff/tests/__init__.py` — empty file.

`python-bff/tests/conftest.py`:

```python
from __future__ import annotations
import pytest
from httpx import AsyncClient, ASGITransport
from app.main import create_app


@pytest.fixture
def app():
    return create_app()


@pytest.fixture
async def client(app):
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
```

`python-bff/tests/test_health.py`:

```python
from __future__ import annotations
import pytest


@pytest.mark.asyncio
async def test_liveness(client):
    resp = await client.get("/api/v1/health/liveness")
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "ok"


@pytest.mark.asyncio
async def test_readiness(client):
    resp = await client.get("/api/v1/health/readiness")
    assert resp.status_code == 200
    data = resp.json()
    assert "database" in data["checks"]
    assert "redis" in data["checks"]
    assert "minio" in data["checks"]
```

**Step 8: Verify app starts**

```powershell
cd python-bff
.venv\Scripts\activate
python -c "from app.main import app; print('App created:', app.title)"
uvicorn app.main:app --host 0.0.0.0 --port 8000 &
# Wait 3 seconds, then:
curl http://localhost:8000/api/v1/health/liveness
# Expected: {"status":"ok"}
curl http://localhost:8000/api/v1/health/readiness
# Expected: {"status":"ok","checks":{"database":"ok","redis":"ok","minio":"ok"}} (all services must be running)
```

**Step 9: Run tests**

```powershell
cd python-bff
.venv\Scripts\activate
python -m pytest tests/ -x -v
```
  </action>
  <verify>
    <automated>cd python-bff && .venv\Scripts\activate && python -m pytest tests/test_health.py -x -v</automated>
  </verify>
  <done>
    - create_app() factory produces a FastAPI instance
    - GET /api/v1/health/liveness returns {"status": "ok"}
    - GET /api/v1/health/readiness returns DB + Redis + MinIO connectivity status
    - Stub uploads.py and tasks.py routers exist (for Plan 01-02 to implement)
    - Router aggregation in api/v1/router.py includes health, uploads, tasks
    - Tests pass: test_liveness and test_readiness
    - uvicorn starts without import errors
  </done>
</task>

</tasks>

<verification>
1. `cd python-bff && .venv\Scripts\activate && uvicorn app.main:app --port 8000` starts without errors
2. `curl http://localhost:8000/api/v1/health/liveness` returns 200 + `{"status":"ok"}`
3. `curl http://localhost:8000/api/v1/health/readiness` returns 200 + connectivity checks for DB, Redis, MinIO
4. `alembic upgrade head` runs successfully against MySQL
5. `python -m pytest tests/ -x -v` passes all tests
</verification>

<success_criteria>
- FastAPI app starts via uvicorn with no import/config errors
- Health check endpoint returns 200 with per-component status (database, redis, minio)
- Alembic async migration generates and applies against MySQL
- All settings load from .env via pydantic-settings (D-07)
- Directory structure matches D-01 (api/, services/, models/, schemas/, core/, workers/, providers/)
- Base ORM model provides id (UUID), tenant_id, created_at, updated_at
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>


