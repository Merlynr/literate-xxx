---
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - wx-fe/src/pages/index/index.vue
  - wx-fe/src/pages/my/index.vue
  - wx-fe/src/pages/generate/index.vue
  - wx-fe/src/stores/generation.ts
  - wx-fe/src/stores/user.ts
  - wx-fe/src/api/generation.ts
  - wx-fe/src/api/auth.ts
  - wx-fe/src/types/generation.ts
  - wx-fe/src/components/quota/QuotaCard.vue
  - wx-fe/src/components/works/WorksList.vue
  - wx-fe/src/components/privacy/PrivacyAgreementCard.vue
  - wx-fe/src/components/generate/AlbumActions.vue
autonomous: true
requirements_addressed:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - QUOTA-04
---

# Phase 4 Plan 03: Home, My, and Generate Frontend UX Completion

## Must Haves

- Show quota on the home page and the "My" page.
- Turn the "My" page into a real works/history screen with pull-to-refresh.
- Require privacy acceptance before generation submission.
- Support save-to-album with permission precheck and fallback instructions.
- Keep the existing mini-program brand language and current generation flow intact.

## Tasks

<task id="04-03-01">
  <objective>Surface quota and shortcut actions on the home page and my page using reusable UI blocks.</objective>
  <read_first>
  - `wx-fe/src/pages/index/index.vue`
  - `wx-fe/src/pages/my/index.vue`
  - `wx-fe/src/pages.json`
  - `wx-fe/src/stores/user.ts`
  - `wx-fe/src/api/auth.ts`
  </read_first>
  <action>
  Replace the current home login bar with a quota summary card that shows remaining quota, frozen quota, and a primary CTA into the generation page.

  Replace the current placeholder "My" menu with a profile header plus a quota summary block that uses the same backend quota endpoint as the home page.
  </action>
  <acceptance_criteria>
  - `wx-fe/src/pages/index/index.vue` contains visible strings for remaining quota and the generation CTA
  - `wx-fe/src/pages/my/index.vue` contains visible strings for remaining quota and works/history entry points
  - both pages still call `userStore.ensureAuth()` on mount
  </acceptance_criteria>
</task>

<task id="04-03-02">
  <objective>Complete the generation wizard with privacy gating, album actions, and explicit result affordances.</objective>
  <read_first>
  - `wx-fe/src/pages/generate/index.vue`
  - `wx-fe/src/stores/generation.ts`
  - `wx-fe/src/api/generation.ts`
  - `wx-fe/src/components/generate/GenerateStepper.vue`
  - `wx-fe/src/components/generate/UploadCard.vue`
  - `wx-fe/src/components/generate/ProgressPanel.vue`
  - `wx-fe/src/components/generate/ResultPanel.vue`
  </read_first>
  <action>
  Add a privacy agreement card or gate to the generation page that must be accepted before `startGeneration()` can submit.

  Add album-saving actions to the success state:
  - `wx.authorize({ scope: 'scope.writePhotosAlbum' })`
  - `wx.saveImageToPhotosAlbum`
  - `wx.openSetting` fallback when permission is denied

  Keep the existing generate flow but extend the success state so the user can preview, save the raw image, and save the watermarked image.
  </action>
  <acceptance_criteria>
  - `wx-fe/src/pages/generate/index.vue` contains a privacy acceptance control and album-save actions
  - `wx-fe/src/stores/generation.ts` blocks submission until the privacy state is accepted
  - the code references `saveImageToPhotosAlbum` and `openSetting`
  - `vue-tsc --noEmit` succeeds after the page and store updates
  </acceptance_criteria>
</task>

<task id="04-03-03">
  <objective>Turn the My page into a pull-to-refresh works list backed by the generation history API.</objective>
  <read_first>
  - `wx-fe/src/pages/my/index.vue`
  - `wx-fe/src/stores/generation.ts`
  - `wx-fe/src/api/generation.ts`
  - `wx-fe/src/types/generation.ts`
  - `python-bff/app/api/v1/generation_history.py`
  </read_first>
  <action>
  Add a works list with:
  - thumbnail preview
  - status badge
  - timestamp
  - tap-to-open result preview
  - pull-to-refresh on the list container

  Wire the list to the generation history API and reuse the signed result URLs instead of inventing a second asset source.
  </action>
  <acceptance_criteria>
  - `wx-fe/src/pages/my/index.vue` contains a refreshable works list
  - the list row markup includes thumbnail, status text, and timestamp text
  - tapping an item can open a preview for an existing result URL
  - `vue-tsc --noEmit` still exits 0 after the works list lands
  </acceptance_criteria>
</task>

## Verification

- `vue-tsc --noEmit` exits 0.
- Home, generate, and my pages still load under the existing tab bar.
- The generation page prevents first-time submission until privacy is accepted.
- Successful jobs can be saved to the photo album through a permission-aware flow.
