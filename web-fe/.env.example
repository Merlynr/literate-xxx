# BFF API base (must include /api/v1)
VITE_API_BASE_URL=http://8.141.7.56:8000/api/v1

# Local dev via Vite proxy (avoids CORS): use /api/v1 and set proxy in vite.config.ts
# VITE_API_BASE_URL=/api/v1
